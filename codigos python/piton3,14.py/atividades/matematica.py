import math
# a = float(input('digite um numero real: '))
# m = math.trunc(a)
# print('a parte inteira de {} é: {}'.format(a, m))
# catetos e hipotenusa
# o = float(input('cateto oposto: '))
# a = float(input('cateto adjacente: '))
# h = pow(o,2) + pow(a,2)
# r = math.sqrt(h)
# print('o valor da hipotenusa é: {:2f}'.format(r))
# hi = math.hypot(o, a)
# print('o valor da hipotenusa é: {}'.format(hi))
# sen, cos, tan
# q = float(input('ângulo: '))
# math.radians(q) = converter radiano. necessário pois o sin,cos,tan estão em radianos
# s = math.sin(math.radians(q))
# c = math.cos(math.radians(q))
# t = math.tan(math.radians(q))
# print('o sen: {:.2f},cos: {:.2f},tan: {:.2f}'.format(s,c,t))
